// Template — copy to secrets.ts and fill in the real value.
// secrets.ts is gitignored; the real value must match the MOBILE_APP_SECRET
// env var on the backend (Railway).
export const APP_SECRET = 'your-app-secret-here';
